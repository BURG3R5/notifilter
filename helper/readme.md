# Notification Helper

A minimal application for testing NotiFilter.

This app is not intended for distribution or use.

## Features

- When the launcher icon is tapped, a notification is sent with the title "N" and no content.
- The app has two shortcuts labeled "A" and "B", which both send notifications with the same title
  as their labels and no content.
